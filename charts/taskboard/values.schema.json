{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "replicaCount": { "type": "integer", "minimum": 1 },
    "image": {
      "type": "object",
      "required": ["repository", "pullPolicy"],
      "properties": {
        "repository": { "type": "string", "minLength": 1 },
        "pullPolicy": { "enum": ["Always", "IfNotPresent", "Never"] },
        "tag": { "type": "string" },
        "digest": { "type": "string", "pattern": "^$|^sha256:[a-f0-9]{64}$" }
      }
    },
    "database": {
      "type": "object",
      "required": ["existingSecret", "urlKey"],
      "properties": {
        "existingSecret": { "type": "string", "minLength": 1 },
        "urlKey": { "type": "string", "minLength": 1 }
      }
    },
    "secrets": {
      "type": "object",
      "required": ["existingSecret", "authTokenKey"],
      "properties": {
        "existingSecret": { "type": "string", "minLength": 1 },
        "authTokenKey": { "type": "string", "minLength": 1 }
      }
    },
    "metrics": {
      "type": "object",
      "properties": {
        "enabled": { "type": "boolean" },
        "port": { "type": "integer", "minimum": 1024, "maximum": 65535 }
      }
    },
    "taskboard": {
      "type": "object",
      "properties": {
        "browserAuthMode": { "enum": ["oidc", "cloudflare_access"] },
        "leaseSeconds": { "type": "integer", "minimum": 30, "maximum": 3600 },
        "mcpDefaultType": { "enum": ["personal", "work"] },
        "allowedHosts": { "type": "array", "minItems": 1, "items": { "type": "string", "minLength": 1 } },
        "retentionDays": { "type": "integer", "minimum": 0, "maximum": 36500 },
        "roles": {
          "type": "object",
          "required": ["default", "ownerGroups", "adminGroups", "memberGroups", "viewerGroups"],
          "properties": {
            "default": { "enum": ["owner", "admin", "member", "viewer"] },
            "ownerGroups": { "type": "array", "items": { "type": "string", "minLength": 1 } },
            "adminGroups": { "type": "array", "items": { "type": "string", "minLength": 1 } },
            "memberGroups": { "type": "array", "items": { "type": "string", "minLength": 1 } },
            "viewerGroups": { "type": "array", "items": { "type": "string", "minLength": 1 } }
          }
        },
        "agentPolicy": {
          "type": "object",
          "required": ["capabilities", "maxConcurrentRuns", "maxPickupsPerMinute", "maxRunSeconds", "requireIdempotency", "policiesJSON"],
          "properties": {
            "capabilities": { "type": "array", "items": { "enum": ["task:read", "task:create", "task:claim", "task:update", "task:complete", "task:sensitive", "template:read", "template:manage"] } },
            "maxConcurrentRuns": { "type": "integer", "minimum": 1, "maximum": 100 },
            "maxPickupsPerMinute": { "type": "integer", "minimum": 1, "maximum": 1000 },
            "maxRunSeconds": { "type": "integer", "minimum": 60, "maximum": 604800 },
            "requireIdempotency": { "type": "boolean" },
            "policiesJSON": { "type": "string" }
          }
        },
        "webhook": {
          "type": "object",
          "required": ["url", "maxAttempts"],
          "properties": {
            "url": { "type": "string", "pattern": "^$|^https://" },
            "maxAttempts": { "type": "integer", "minimum": 1, "maximum": 100 }
          }
        }
      }
    }
  },
  "required": ["replicaCount", "image", "database", "secrets", "taskboard"]
}
